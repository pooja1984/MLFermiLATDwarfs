Web scraper Notebook to upload files from the FEMILAT telescope webpage. 

Requirements:

- have your Rucio-client environment installed.
  If you dont, follow the documentation at: https://datalake-rucio.docs.cern.ch/
- Run 
  $ python3 -m pip install papermill

Before runnin gthe notebook, make sure you can execute the command 'rucio whoami' from your terminal. 
The, you can execute the notebook and see its output by running the auto.sh script with:
$ bash auto.sh 
You can automate the running of the notebook with a cron job. 
